# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

.adr/

# Raw media pulled from the old WordPress site. Kept locally as an archive;
# anything used on the site is processed and committed under public/images/.
wp-media/

# Playwright MCP scratch output (page snapshots, screenshots). Browser
# verification is how this repo checks layout and the render lab, so these
# accumulate on every session; one got committed in 5aa0914 before this line
# existed.
.playwright-mcp/
